<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.ReplyDAO">
	<!-- 댓글 전체 조회 -->
	<select id="selectAllByBuildNo" parameterType="int" resultType="ReplyVO">
		select no, content, user_no as userNo, nickname, password, to_char(reg_date, 'yyyy-mm-dd/hh:mm:ss') as regDate, build_no as buildNo
		  from ssh_reply
		 where build_no = #{buildNo}
	</select>
	
	<!-- 댓글 등록 -->
	<insert id="insertReply" parameterType="ReplyVO">
		insert into ssh_reply(no, content, user_no, nickname, password, build_no)
		values(seq_ssh_reply_no.nextval, #{content}, #{userNo, jdbcType=NUMERIC}, #{nickname}, #{password, jdbcType=VARCHAR}, #{buildNo})
	</insert>
</mapper>